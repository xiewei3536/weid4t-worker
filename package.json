{
  "name": "weitv-control",
  "version": "1.0.0",
  "description": "偉電視安卓盒 App 的直播源同步控制平面（Cloudflare Worker + KV）",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
